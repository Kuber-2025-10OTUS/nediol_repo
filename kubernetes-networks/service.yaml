apiVersion: v1
kind: Service
metadata:
  name: homework-service
  namespace: homework
spec:
  type: ClusterIP
  selector:
    myapp: homework
  ports:
  - name: homework-service-port
    protocol: TCP
    port: 8000
    targetPort: 8000